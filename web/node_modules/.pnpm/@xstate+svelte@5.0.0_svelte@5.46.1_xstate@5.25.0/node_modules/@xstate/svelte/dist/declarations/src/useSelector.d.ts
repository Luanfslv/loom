import type { AnyActorRef } from 'xstate';
export declare const useSelector: <TActor extends Pick<AnyActorRef, "getSnapshot" | "subscribe">, T>(actor: TActor, selector: (snapshot: TActor extends {
    getSnapshot(): infer TSnapshot;
} ? TSnapshot : undefined) => T, compare?: (a: T, b: T) => boolean) => import("svelte/store").Readable<T>;
